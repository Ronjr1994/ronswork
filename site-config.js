/* Ron's Work deployment configuration.
   The site works immediately with email composition.
   Add a secure HTTPS endpoint later to enable direct form delivery. */
window.RONS_WORK_CONFIG = Object.freeze({
  contactEmail: "ronjr.dialino@gmail.com",
  intakeEndpoint: "",
  siteUrl: "https://ronjr1994.github.io/ronworks/"
});
